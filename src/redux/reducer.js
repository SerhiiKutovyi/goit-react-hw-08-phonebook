// import { USER_ADD, USER_DELETE } from './types';

// export const userReducer = (state = [], { type, payload }) => {
//   switch (type) {
//     case USER_ADD:
//       return [...state, payload];

//     case USER_DELETE:
//       return [state.filter(data => data.id !== payload)];

//     default:
//       break;
//   }
// };
